{% comment %} Default page {% endcomment %}
{% render 'page-hero', heading: page.title, eyebrow: 'TLUXE Hairs' %}
<div style="padding-top:80px;min-height:60vh;">
  <div class="container" style="max-width:800px;">
    <div style="color:var(--cream-dim);line-height:1.9;">
      {{ page.content }}
    </div>
  </div>
</div>
